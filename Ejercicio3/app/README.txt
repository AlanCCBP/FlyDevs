Command for compiling SCSS stylesheets: npm run scss. Please, let it run in the cmd.
When a change is made (and saved) into a *.SCSS file, Watch, that is looking for changes permanently, will compile the SCSS stylesheets.

Thanks!

Alan (: